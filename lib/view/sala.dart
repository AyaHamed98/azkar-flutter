import 'package:flutter/material.dart';

class Sala extends StatelessWidget {
  const Sala({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("3"));
  }
}
